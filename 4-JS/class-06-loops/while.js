    let count = 0; // flag
    while (count <= 500) {
    if (count % 2 === 0) {
    console.log(count);
    }
    count++; // container 
}

count = 0;
let noia = 0;
while (noia <= 500) {
console.log(noia);
noia = count * 2;
count++;
}